pre-commit:
  jobs:
    - name: biome-root
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: bunx --no-install biome check {staged_files}
    - name: biome-nested
      glob: "**/*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: bunx --no-install biome check {staged_files}
